import React, { FunctionComponent } from 'react';

import MainContainer from './Main';

const App: FunctionComponent = () => {
  return (
    <div>
      <MainContainer />
    </div>
  );
};

export default App;